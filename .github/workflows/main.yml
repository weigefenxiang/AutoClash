name: AutoClash

on: [push]

jobs:
  job1:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    - run: python3 -V
    - run: pwd
    - run: docker build -t ubuntu/clash:0.1 docker/.
    - run: docker images
    - run: docker ps -a





